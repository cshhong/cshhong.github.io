# Test Locally: Run the site locally to verify:
bundle exec jekyll serve

post.url value is automatically generated by Jekyll based on the permalink structure defined in your _config.yml file or the front matter of the post itself.

# clear Jekyll cache
rm -rf _site

# build Jekyll
bundle exec jekyll serve